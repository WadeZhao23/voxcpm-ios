# 复制为 .env 并按需修改（或直接 export 这些环境变量）
VOXCPM_MODEL_ID=openbmb/VoxCPM2
VOXCPM_DEVICE=auto          # auto / cpu / mps / cuda / cuda:0
VOXCPM_OPTIMIZE=false       # Mac(MPS) 上建议 false；有 NVIDIA GPU 可设 true 用 torch.compile 加速
VOXCPM_LOAD_DENOISER=false  # 是否加载参考音频降噪器（会多下一个模型）
