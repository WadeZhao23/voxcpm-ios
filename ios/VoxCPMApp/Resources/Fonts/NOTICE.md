# 字体说明

本目录的 OPPO Sans 字体（`OPPOSans-R/M/B/L/H.ttf`）是 OPPO 官方发布的 **免费商用** 字体，
用于实现 App 的 Ego 设计风格基调字体。

- 5 个字重在 ttf 中是 **5 个独立 family**（PostScript：`OPPOSans-R/M/B/L/H`），
  通过 `Font.app(size:weight:)` 取用（见 `Theme/Font+App.swift`）。
- 版权与许可归 OPPO 所有，使用请遵循其官方字体许可协议。
